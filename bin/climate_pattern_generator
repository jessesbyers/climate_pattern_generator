#!/usr/bin/env ruby

require './lib/climate_pattern_generator'
require 'sqlite3'

DB = {:conn => SQLite3::Database.new('./lib/climate_pattern_generator/colors.db')}

ClimatePatternGenerator::Color.all

ClimatePatternGenerator::CLI.new.call
